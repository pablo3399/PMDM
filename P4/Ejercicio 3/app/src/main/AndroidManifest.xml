<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.entrenamiento">

    <application
        android:name=".Core.ListaDeEntrenamientos"
        android:allowBackup="true"
        android:icon="@mipmap/water"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/water"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.AddTraining"></activity>
        <activity android:name=".UI.MostrarStats"></activity>
    </application>

</manifest>